export enum PostType {
  post = 'post',
  reply = 'reply',
  repost = 'repost',
  quotePost = 'quotePost',
}

export enum PostVisibility {
  PUBLIC = 'public',
  FOLLOWERS = 'followers',
  CIRCLE = 'circle',
  COMMUNITY = 'community',
  PROTECTED = 'protected',
}
